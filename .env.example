# Intervals.icu API Configuration

# Base URL (Optional, defaults to https://intervals.icu/api/v1)
# INTERVALS_API_BASE_URL=https://intervals.icu/api/v1

# Required: Your Intervals.icu API Key
API_KEY=your_intervals_api_key_here

# Required: Your Intervals.icu Athlete ID
ATHLETE_ID=your_athlete_id_here

# The API_KEY and ATHLETE_ID will be used as defaults for all API calls
# when these parameters are not explicitly provided to the tool functions.

# Server Configuration (for remote MCP server)
# Host to bind to (Optional, defaults to 0.0.0.0)
# HOST=0.0.0.0

# Port to bind to (Optional, defaults to 8000)
# PORT=8000

# Security Configuration (for Remote MCP)
# API key for MCP server authentication (Optional, if not set, no auth required)
# MCP_API_KEY=your_secure_mcp_api_key_here

# Allowed origins for CORS (Optional, defaults to * in development)
# ALLOWED_ORIGINS=https://claude.ai,https://app.claude.ai

# Enforce HTTPS connections (Optional, defaults to false)
# ENFORCE_HTTPS=true

# Logging level (Optional, defaults to INFO)
# LOG_LEVEL=INFO

# OAuth 2.1 Configuration (Remote MCP用)
# OAuth Authorization Server URL (外部認証サーバーを使用する場合)
# OAUTH_AUTHORIZATION_SERVER=https://your-auth-server.com

# OAuth Client credentials
# OAUTH_CLIENT_ID=your_oauth_client_id
# OAUTH_CLIENT_SECRET=your_oauth_client_secret

# OAuth scopes
# OAUTH_SCOPE=intervals:read intervals:write intervals:admin

# JWT Configuration
# JWT_SECRET_KEY=your_jwt_secret_key_here
# JWT_ALGORITHM=HS256
# OAUTH_AUDIENCE=intervals-mcp-server

# Server base URL (for metadata generation)
# BASE_URL=https://your-mcp-server.com

# OAuth endpoints (if using external authorization server)
# OAUTH_TOKEN_ENDPOINT=https://your-auth-server.com/oauth/token
# OAUTH_AUTHORIZATION_ENDPOINT=https://your-auth-server.com/oauth/authorize
# OAUTH_JWKS_URI=https://your-auth-server.com/.well-known/jwks.json
